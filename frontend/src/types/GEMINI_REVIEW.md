## [2026-04-30 10:20:08] `D:\develop\bangsan\frontend\src\types\index.ts`

제공해주신 `frontend/src/types/index.ts` 파일에 대한 코드 리뷰 결과입니다.

### 1. 코드 품질 및 개선 제안

*   **Severity 명칭 일관성:** `Severity`에서 `MED`를 사용하고 있습니다. 일반적으로 `MEDIUM`을 더 많이 사용하므로, 백엔드의 `Severity.java` 정의와 일치하는지 확인이 필요합니다. (보통 백엔드 Enum이 `MEDIUM`이면 프론트엔드도 맞추는 것이 유지보수에 유리합니다.)
*   **날짜 타입 명시:** `occurredAt`, `detectedAt`이 `string`으로 정의되어 있습니다. ISO 8601 형식을 기대한다면 주석을 추가하거나, 필요 시 `Date` 객체로 변환하여 사용하는 로직을 고려해 보세요.
*   **상수(Enum)화 권장:** 현재는 Union Type으로만 정의되어 있습니다. 런타임에서 특정 타입에 따른 필터링이나 레이블 출력이 자주 발생한다면, `const enum`이나 객체 리터럴을 병행하여 값 자체를 안전하게 참조할 수 있게 하는 것이 좋습니다.
*   **Optional 타입 최적화:** `ApiResponse`의 `meta`가 `null`일 수 있는데, 데이터가 단건인 경우와 목록인 경우를 명확히 분리하기 위해 `meta`를 선택적 속성(`meta?: ...`)으로 정의하는 것이 더 깔끔할 수 있습니다.

### 2. 백엔드 연동 체크 포인트 (버그 예방)

*   **AlertType 접두어:** `AlertType`의 값들이 `ALERT_`로 시작합니다. 백엔드(`AlertType.java`)에서도 동일한 접두어를 사용하는지 반드시 대조해 보시기 바랍니다.
*   **ID 타입:** `id`가 `number`로 되어 있습니다. 만약 백엔드에서 `Long` 타입을 사용하고 데이터 양이 매우 많아질 경우(2^53 - 1 초과), JavaScript의 `number` 범위를 벗어날 수 있으므로 주의가 필요합니다.

### 3. 총평
전반적으로 인터페이스 구조가 깔끔하고 TypeScript의 특징을 잘 활용하고 있습니다. 백엔드 도메인 모델과의 **타입 일관성(Naming, Enum Value)**만 재확인하면 안정적인 개발이 가능할 것으로 보입니다.

---

